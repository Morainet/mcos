org.gradle.jvmargs=-Xmx2048m -Dfile.encoding=UTF-8
android.useAndroidX=true
kotlin.code.style=official
android.nonTransitiveRClass=true

# Maven publication coordinates (all artifacts share one group + version).
# io.github.morainet is verified on Maven Central via GitHub org ownership
# (Morainet). If a dedicated domain is bought later, switch to com.morainet,
# re-verify the namespace on the Central Portal, and migrate artifacts.
mcosGroupId=io.github.morainet
mcosVersion=0.2.0-SNAPSHOT
