# Changelog

All notable changes to **Mobile Command OS (MCOS)** are documented here.

The format is based on [Keep a Changelog](https://keepachangelog.com/en/1.1.0/),
and this project adheres to [Semantic Versioning](https://semver.org/spec/v2.0.0.html)
for the Command Protocol and Runtime API. See [docs/en/02-command-protocol.md](./docs/en/02-command-protocol.md) §14 for versioning details.

---

## [Unreleased]

### Implemented — P1 MVP + P2 core (2026-08)
The repository has moved from design-only to a working implementation:

- **Build**: Gradle Kotlin DSL multi-module (JDK 17, Kotlin 2.0.21, AGP 8.7.3, compileSdk 35, minSdk 26); modules `mcos-sdk`, `mcos-runtime`, `mcos-android`, `plugins/mcos-plugin-{hello,system,camera,files}`.
- **Command pipeline**: `DslParser` (lexer/parser/canonicalizer, all 8 golden fixtures pass), `ExecutionIr`, `CommandRegistry`, `Executor` (steps, artifacts, confirmation, cancellation), JSON-Schema validation.
- **Security**: `PermissionKernel` (`decideConfirmation` NORMAL/ELEVATED), `NetworkEgressPolicy` (`decideEgress`), `PromptInjectionDetector`, `RateLimiter` (per-plugin/min), `AuditLog` (append/filter/rotate, sha256 + HMAC chain), `AuthStampSigner`.
- **Workflow** (P2 full): `WorkflowEngine` — sequential/parallel/if/loop/retry/try/confirm; named `WorkflowStore`; `WorkflowJson` decode; wired into `McosRuntime.runWorkflow` with plan preview.
- **Event Bus** (P2 full): typed `EventEnvelope` + `EventFilter` (typePrefix + deep-equality `where`), per-subscriber isolation under `SupervisorJob`, bounded channels with drop-oldest backpressure + `EventAuditSink`; P1 run-event channel kept.
- **Memory** (P2 core): TTL, semantic tags, 4-step fuzzy `resolveRef` with confidence + reason, `CREATED`/`UPDATED`/`CONFLICT` write semantics with cross-path dedup and superseded history.
- **LLM**: `LlmPlanner`, `OpenAiLlmProvider`, `ChatOrchestrator` (runtime side; Android UI not connected).
- **Android shell**: Jetpack Compose CLI/Chat UI, `AndroidHostServices` (notifications, camera, files), `ActivityResultBridge`.
- **Tests**: 361 across all modules (fixtures, executor, permission, audit, workflow W1–W6, event bus ×8, memory M1–M33, plugins, Android).
- **Docs**: `docs/en/11-implementation-status.md` rewritten from "design-only" to the live status matrix above.

### Security P1 closure (2026-08-12)
- **Secret Management** (§9.2): `{{secret.<key>}}` template resolution — `security/SecretResolver` + per-plugin `Executor` NetService decorator. Values are resolved from the plugin-scoped `SecureStore` only at outbound-request time, are **never** written back into `ExecutionContext.args`, and unknown keys stay inert; audit redaction honors the `x-mcos-secret` marker (§9.4).
- **Crash-loop Quarantine** (§15.3): `security/CrashQuarantine` — ≥3 uncaught plugin exceptions within a 60 s sliding window quarantine the plugin (commands unregistered + audit event `plugin.quarantined`); successful invokes reset the window; only explicit re-enable or a new verified version lifts the quarantine.
- **Tests**: +24 (385 total) — `SecretResolver`, `CrashQuarantine`, `Executor` security integration (A-1/A-2), audit `x-mcos-secret` redaction.

### Memory P2 closure (2026-08-12)
- **Episodic Memory** (07 §8): `memory/EpisodicMemory` — run-summary store with time-decay recall (0–7d ×1.0, 7–30d ×0.5, 30–90d ×0.2, >90d ×0.05, §8.1), Planner first-use check (§8.3), 90-day retention + 50→5 auto-summarize (§8.2), `MemoryExport`-shaped JSON export.
- **Shared similarity**: `fuzzyScore` extracted into `memory/FuzzyScore` (stand-in embedding, §6.0 Step 3) and reused by both `MemoryStore` and `EpisodicMemory`.
- **Tests**: +14 (399 total) — episodic `EpisodicMemoryTest` E1–E14; all M1–M33 fuzzy-ref tests stay green on the shared scorer.

### Episodic Memory wired into run completion (2026-08-13)
- **RunSummarizer** (07 §9.4): `memory/RunSummarizer` — `McosRuntime.runCommands`/`runWorkflow` now record an `EpisodicRecord` at every terminal outcome (SUCCESS / FAILED / CANCELLED). Summary text comes from the raw DSL payload (truncated to 160 chars, single-line), falling back to the command-id listing; entities are memory paths (`people.tom`, `places.office`, …) extracted from command args under known namespaces.
- **Builder**: `McosRuntime.Builder.withEpisodicMemory(...)` + `episodicMemory()` accessor (defaults to a fresh `EpisodicMemory`); planner first-use check (§8.3) reachable as `runtime.episodicMemory().hasExecuted(...)`.
- **Tests**: +11 — `RunSummarizerTest` S1–S11: summary building, namespace-scoped entity extraction (incl. nested args), outcome recording, and full `McosRuntime` integration for DSL / failing / workflow runs.

### Multi-provider Planner probes (2026-08-13)
- **Capability model** (06 §3.0): `Capability` enum (`CHAT` / `PLAN` / `TOOL_CALL` / `EMBED`) + `LlmProvider.id` / `capabilities` (defaulted → fully backward compatible) + `LlmProbeResult` health model.
- **`LlmProviderRegistry`** (06 §17 V1): `llm/LlmProviderRegistry` — priority-ordered registration, capability routing (`chatProviders` / `withCapability`), and `suspend healthyProviders()` / `primaryHealthy()` health-probe filtering (§18.1 on-device→cloud routing).
- **Fallback chain**: `LlmPlanner` now accepts `fallbacks: List<LlmProvider>` — a retryable `LlmResponse.Err` from the primary provider walks the chain in order; non-retryable errors stop immediately. Each `LlmPlan` records `providerId` (which provider actually served it).
- **`OpenAiLlmProvider`**: overrides `id = "openai"` and implements `probe()` as a 1-token connectivity check.
- **Tests**: +14 (399 total, full suite green) — `LlmProviderRegistryTest` R1–R8 (registration, duplicate-id rejection, capability filtering, health filtering) + `LlmPlannerFallbackTest` F1–F6 (primary success, retryable→fallback, non-retryable stop, all-fail, multi-hop, no-fallback).

### PlanMode NATIVE_TOOL_CALL (2026-08-13)
- **Tool-calling types** (06 §3.0): `llm/ToolCallTypes` — `ToolCall` / `ToolDescriptor` / `ToolCallResponse` / `TokenUsage` + `PlanMode` enum (`NATIVE_TOOL_CALL` / `FREEFORM_JSON`).
- **Mode selection** (06 §3.2): `LlmPlanner.plan` chooses per provider — `TOOL_CALL` capability → `NATIVE_TOOL_CALL` via `LlmProvider.toolCall`, otherwise `FREEFORM_JSON` (chat + DSL). `LlmProvider.toolCall` defaults to an "unsupported" response, so chat-only providers are unaffected.
- **ToolDescriptor projection**: registry commands projected onto `ToolDescriptor`s with best-effort example DSL→args parsing; `LlmPlan` now records `planMode` alongside `providerId` for audit.
- **`OpenAiLlmProvider`**: advertises `TOOL_CALL` and implements the OpenAI `tools` protocol (`tool_choice=auto`, function schema, `tool_calls` parsing, `usage` tokens).
- **Tests**: +8 (407 total, full suite green) — `LlmPlannerToolCallTest` T1–T8: native path + mode recording, chat-only fallback to FREEFORM_JSON, retryable→chat fallback, non-retryable stop, multi-tool mapping, descriptor projection, empty tool calls, multi-provider tool-call chain.

### Removed
- Removed the Phase-0 code skeleton and build system to make the repository a clean **design-only** baseline. Deleted:
  - All source modules: `mcos-sdk`, `mcos-runtime`, `mcos-android`, `plugins/mcos-plugin-{hello,system,camera}` (7 Kotlin source files + 1 test + `plugin.json`).
  - Gradle build files: `build.gradle.kts`, `settings.gradle.kts`, `gradle.properties`, `gradle/` (wrapper), `gradlew`, `gradlew.bat`, `.gradle/`, `build/`.
- The previous skeleton is recorded historically under [0.1.0] below. Implementation will restart fresh in Phase 1, targeting the specs in `docs/`.

### Docs
- Rewrote status/structure across docs to reflect the design-only state:
  `README.md`, `README.zh-CN.md`, `CONTRIBUTING.md`, `docs/00-vision.md` §5, `docs/01-architecture.md` §14, `docs/04-plugin-sdk.md` §17, and the 🚧 notes in `docs/02/03/04/05`.
- Added [docs/en/11-implementation-status.md](./docs/en/11-implementation-status.md) — spec-vs-implementation roadmap.
- Added [docs/en/REPOSITORIES.md](./docs/en/REPOSITORIES.md) — target module dependency index.
- Added [README.zh-CN.md](./README.zh-CN.md) — Chinese entry point.
- Added golden negative fixtures (`06-nested-call`, `07-positional-arg`, `08-malformed`).
- Clarified `dslVersion` as `MAJOR.MINOR` shorthand in [02](./docs/en/02-command-protocol.md) §14.2; added string-escape rules §18.1.

### Docs — Bilingual split
- Reorganized the doc tree into a **full bilingual** layout: `docs/en/` (English, authoritative) + `docs/zh/` (Chinese mirror) + shared `docs/fixtures/`.
- Translated all 12 RFCs + `README.md` + `REPOSITORIES.md` into Simplified Chinese under `docs/zh/`. Code, JSON, command IDs, error codes, field names, ABNF, and Mermaid kept in English; prose translated with first-use "中文（English）" glossing.
- Added a language switcher (`> **语言:**`) at the top of every `docs/zh/` file linking back to its English counterpart.
- Updated `README.md`, `README.zh-CN.md`, and `CONTRIBUTING.md` to point at the new `docs/en/` and `docs/zh/` paths. CONTRIBUTING now requires doc changes to keep both languages in sync.

### Docs — Architecture expansion (merged into 01-architecture.md)
- Expanded [docs/en/01-architecture.md](./docs/en/01-architecture.md) (+ Chinese mirror) from a high-level overview into a **single combined** architecture document covering both the logical layering and the implementation-level contracts. The merged doc now closes 9 spec gaps by specifying: App↔Runtime IPC contract (§7, AIDL method table + Parcelable rules), coroutine Dispatcher assignment (§8, per-component threading map + structured concurrency), the 10-stage Runtime execution pipeline (§9, including the new explicit **Stage 4 — Expand** for sugar/Memory-ref resolution), the unified `HostServices` facade (§11.1, reconciling the `PluginHost`/`HostServices` naming split), `ExecutionContext` typed accessors (§11.2), the `Artifact` type (§11.3), the `RuntimeEvent` 11-variant sealed class (§11.5), the `McosErrorCode` unified enum + failure-domain matrix (§15), the Grant `scope` vocabulary (§10.2), and a gap-closure table (§20) tracking which RFCs should adopt each resolution. Includes full Kotlin signatures, mermaid sequence diagrams, and a P1 implementation checklist (§17.1). Does **not** modify the normative RFCs — conflicts deferred to RFC revisions.

### Docs — Command Protocol expansion (merged into 02-command-protocol.md)
- Expanded [docs/en/02-command-protocol.md](./docs/en/02-command-protocol.md) (+ Chinese mirror) from a ~660-line normative sketch into a ~990-line **implementable RFC** by inserting 9 new subsections in place (no companion files, per the one-doc-per-topic convention). The added detail closes the gaps needed to write a parser, validator, and registry directly from the spec: §4.4 namespace registration & conflict arbitration (priority table + duplicate detection), §4.5 aliases (descriptor `aliases[]`, resolution, audit recording), §5.4 type → JSON-Schema mapping & validation bounds (per-primitive limits, `duration` disambiguation, `bytes` cap, `ref` resolution order), §6.6 lexer token specification (token table + regexes + whitespace/BOM rules), §6.7 error location precision (1-indexed code-point columns), §6.8 number literal bounds (int overflow, no exponents, no leading zeros), §6.9 string literal bounds (65536 code points, NFC, no bare surrogates), §7.5 canonicalization algorithm (normative recursive pseudocode — keys sorted, arrays not, `meta` excluded), §8.2 `meta` field spec (fixed closed key set + injector ownership), §8.3 per-error-code `details` schema table, §9.5 pipeline cross-reference (this RFC's 9 steps ↔ architecture's 10 stages), §9.6 transactional boundary (clean pre-execute vs non-rollbackable execute), §12.4 MCP tool → MCOS schema conversion table (fails-closed on unmapped types), §12.5 app-function package-name `.`→`_` encoding, §12.6 mandatory Intent extras schema, and §16.1 structured conformance test matrix (existing 8 fixtures + 7 suggested cases). Frozen contracts (ABNF, type table, IR JSON shape, error codes, descriptor field names) are unchanged.

### Docs — Runtime expansion (merged into 03-runtime.md)
- Expanded [docs/en/03-runtime.md](./docs/en/03-runtime.md) (+ Chinese mirror) from a ~480-line sketch into a ~850-line **implementable subsystem design** by inserting 11 new subsections in place (per the one-doc-per-topic convention). Strategy: **cross-reference rather than duplicate** — content already at spec-level in [01-architecture.md](./docs/en/01-architecture.md) (10-stage pipeline, Dispatcher table, `HostServices`/`ExecutionContext`/`AuthStamp`/`RuntimeEvent`, `McosErrorCode` enum + failure-domain matrix + circuit breaker, `CommandDescriptor` schema, App↔Runtime IPC `RuntimeFacade`) and in [02](./docs/en/02-command-protocol.md) (grammar, canonicalization, error `details`) is compressed to pointers; the new material covers runtime-internal mechanics absent from both: §3.1 startup/shutdown sequencing + foreground-pin state machine, §4 reconciliation of the `McosRuntime`/`RuntimeFacade` split + `RuntimeEvent` 10-vs-11 variant drift (now defers to arch §11.5), §5.1 parser internal pipeline (lexer→parser→AST→codec), the `ExecutionIr` in-memory sealed class, fail-fast error strategy, codec round-trip contract, sugar-macro registry, §6.4 Registry internal indexes (by-id `SortedSet` for version coexistence, by-alias, by-namespace trie) + version-selection algorithm, §6.5 hot-reload install/unload drain mechanics, §8.4 Scheduler bounded-channel backpressure + `RATE_LIMITED` retry-after, §8.5 per-device serial deadlock prevention (leveled lock acquisition + `CONFLICT` on nested), §9.4 Executor dispatch lifecycle (cooperative-then-forced cancel, cancel-grace, watchdog for non-yielding plugins, activity-result bridging), §9.5 `McosException` declaration (the plugin-declared error channel referenced but undefined in arch §10.3), §11.4 EventBus delivery semantics (at-most-once, subscriber isolation, backpressure drop, cross-process AIDL), §13.3 Audit storage schema (SQL) + deterministic redaction walk algorithm + retention + signed JSONL export, §14.1 PlannerBridge repair-loop contract (`ValidationError` aligned with `SCHEMA_VIOLATION.details`, max-attempt source), §16.1–16.5 plugin loading lifecycle (manifest discovery, offline signature verification caching, per-plugin `DexClassLoader` isolation, namespace conflict arbitration mechanism, circuit-breaker→auto-unload escalation), and §19.1 `RuntimeConfig` validation rules + source precedence + per-field hot-reload semantics. Also reconciled the `PluginHost`/`HostServices` naming split (standardized on `HostServices` per arch §11.1). No content in 01/02/04/05/06/07/08 was modified.

### Docs — Plugin SDK expansion (merged into 04-plugin-sdk.md)
- Expanded [docs/en/04-plugin-sdk.md](./docs/en/04-plugin-sdk.md) (+ Chinese mirror) from a ~470-line author-facing sketch into a ~1000-line **implementable SDK reference** by inserting 12 new subsections in place and fixing **8 drift bugs** uncovered while cross-checking against 01/02/03. Strategy: **cross-reference rather than duplicate** — types already normative in [01](./docs/en/01-architecture.md) (`CommandDescriptor` 15-field schema, `HostServices` with `memory()`, `ExecutionContext` accessors, `McosErrorCode` enum + circuit breaker) and [02](./docs/en/02-command-protocol.md) (schema `x-mcos-*` extensions, error `details` shape B, MCP conversion table) are compressed to pointers; the new material is the author-facing detail that was missing or only sketched: §4.4 plugin- and command-level field tables (Type/Required/Default/Constraint, mirroring arch §10 format), §4.5 `inputSchema` authoring guide (when to use `x-mcos-secret`/`x-mcos-ref`/`x-mcos-semantic`/`x-mcos-default-from-memory`, `threadHint` declaration), §5.2 error declaration guide (`CommandResult.Err` vs throwing `McosException` vs generic `Throwable`, per-code `details` required fields), §5.3 `meta` is Runtime-owned warning, §6 renamed `PluginHost`→`HostServices` with the full facade including `memory()`, §6.1–6.6 **the five service interfaces fully declared for the first time** (`FileService`, `NetService`, `UiService`, `SecureStore`, `Clock`, `MemoryFacade` — previously these had zero method signatures anywhere in the doc set), §7.4 cooperative-cancellation obligation (`ensureActive()` pattern, circuit-breaker consequence), §8.1 MCOS scope vocabulary cross-ref (`command.<id>`/`memory.read`/`network.<domain>`/`mcp.server.<id>`/`securestore.<keyprefix>`), §9.1 plugin lifecycle state machine (`onLoad`→ready→`onUnload`, load-failure registration rollback, auto-unload author awareness, re-enable fresh-instance rule), §10 MCP Adapter rewritten from a 4-bullet sketch into a cross-reference table (responsibilities + pointers to 02 §12.4 and 09 §10), §11.1 the `http` object field-by-field spec (`{{arg.x}}`/`{{secret.<key>}}` templates, `auth` binding to `SecureStore`, `errorMapping` HTTP status → `McosErrorCode`, security constraints), §12.1 i18n locale fallback chain (`zh-CN`→`zh`→default `en`) + localizable-field list + marketplace CI completeness gate, §13.1 CI-checkable versioning rules table (SemVer regex, command-version coupling, `minRuntimeVersion` monotonicity, `replacedBy` resolution), §13.2 `mcos-sdk-gradle` validator check list (mirrors marketplace CI), §14.1 full `mcos-sdk-testing` API (`FakeRuntime.Builder`, `FakeResult`, assertion helpers, complete happy-path + permission-denied example), and a prompt-injection checklist item (08 §11) for plugins producing Planner-consumed text. **8 drift bugs fixed:** `PluginHost`→`HostServices` (5 occurrences), `CommandResult.Err` now carries `details: JsonObject`, `services.memory(): MemoryFacade` added to the facade, `ctx.memoryDefaultCity()`→`ctx.memoryDefault("places.defaultCity")`, `ui().startForResult(...)`→`ui().startActivityForResult(intent)`, `McosException` cross-ref added, `McosErrorCode` reference added for `Err.code`, `onLoad(host: PluginHost)`→`onLoad(services: HostServices)`. No content in 01/02/03/05/06/07/08 was modified.

### Docs — Planner/Agent expansion (merged into 06-agent.md) + CompileResult structured Clarify/Refuse
- Expanded [docs/en/06-agent.md](./docs/en/06-agent.md) (+ Chinese mirror) from a ~319-line sketch (18 sections, most bullet-only, **all key types undefined** — `PlanRequest`/`ProviderPlan`/`ChatRequest`/`Capability` enum/`GoalRequest`/repair state machine/agent loop sequence/provider adapter layer/STT provider interface/prompt-injection marking protocol all missing) into a ~1000-line **implementable Planner/Agent design** by inserting 18 new subsections in place and resolving **10 gaps** discovered during cross-doc exploration. Strategy: **cross-reference rather than duplicate** — types already normative in [01](./docs/en/01-architecture.md) (Planner component table §6.2, `Source` enum §11.6, Registry filtered views §10, `COMPILE_FAILED` §15.1, 10-stage pipeline §4, voice surface §6.1), [02](./docs/en/02-command-protocol.md) (`meta` field spec §8.2, error `details` §8.3, `CommandDescriptor` §8, canonicalization §7.5, multi-statement §6.4), [03](./docs/en/03-runtime.md) (`PlannerBridge` interface §14, `CompileResult` sealed class §14.1, `ValidationError`, "Runtime does not embed vendor SDKs", `ExecutionIr.Sequence` §5.1, performance budgets §17, `Dispatchers.IO`), [05](./docs/en/05-workflow.md) (`CompiledWorkflow` §4.0, `confirm` step §5.7, `ConfirmationPolicy` §10, compile algorithm §11.1, Planner emission rules §13), [07](./docs/en/07-memory.md) (Semantic Index §9, Profile schema §4), [08](./docs/en/08-security.md) (prompt injection rules §11, network egress §12, secrets §9, enterprise §13), and [04](./docs/en/04-plugin-sdk.md) (`FakeRuntime` §14.1, cooperative cancellation §7.4, `SecureStore` §6.4, `http` object §11.1) are compressed to pointers; the new material is the Planner/Agent-internal detail that was missing or only sketched. Key additions: §1.1 **architectural positioning** (Planner is App-side `com.mcos.android.planner`, upstream of Stage 1, Runtime only sees `PlannerBridge`) + §1.2 **Planner vs Agent concept split** (Planner = stateless one-shot compiler `goal→IR` P1; Agent = stateful multi-turn loop Plan→probe→replan→write P2; mermaid `flowchart` showing Agent calls Planner.compile per turn), §3.0 **all 15 missing type definitions for the first time** (`Capability` enum, `ChatRequest`/`ChatResult`/`Message`/`Role`, `PlanRequest`/`PlanResult`, `ToolDescriptor`/`PlannerContext`/`PlanMode`, `ToolCall`/`TokenUsage`, `EmbedRequest`/`EmbedResult`, `ProviderPlan` — the unified intermediate representation, `GoalRequest` cross-ref), §3.2 **Provider Adapter layer** (normative 3-mode decision rule: native tool-use preferred → freeform JSON fallback → constrained decoding V2+; adapter contract collapsing all three into one `ProviderPlan`; 3-mode comparison table), §4.0 PlanRequest field table (Type/Required/Default/Constraint) + §4.1 **catalog retrieval strategy** (7-step normative flow: embed→top-K→recently-used∪pinned→filter→truncate→project, with token budgets tool≤4000/memory≤1000/history≤2000), §5.1–5.5 **five output kinds with JSON shapes** (invoke/sequence/workflow/clarify/refuse — previously 5 one-line bullets; structured `Clarify{question,options?,slots?}` and `Refuse{reason,category,suggestions?}` with `RefuseCategory` 4-value table and UI rendering guide), §6.1 **compile flow pseudocode** (mode-agnostic dispatch on `toolCalls`/`freeformJson`; 5-step validation chain; pure compiler, no I/O) + §6.3 **meta injection** (`source="llm"`/`confidence`/`utteranceId` from 02 §8.2, `correlationId`/`traceId` deferred to Stage 5), §7.1 **repair state machine** (mermaid `stateDiagram-v2`: Compiling→Ok/Repairing/Clarified/Refused with terminal states) + §7.2 repair message format (`ValidationError[]` serialized as TOOL-role message, field-aligned with 02 §8.3) + §7.3 maxRepair limits table (cloud=2/on-device=1, per-utteranceId counting), §8.0 confidence sources (logprobs + heuristics blend) + §8.1 **confirmation trigger rules table** (5 rules: confidence<0.7+destructive→confirm, <0.5+write→clarify, first-use→confirm, network→show URL), §9.0 **system prompt assembly** (normative 5-section order: Role→Tool Catalog→Memory Context→Safety Rules→Output Format, with PlanMode-varying §5) + §9.1 prompt versioning (A/B testing, rollback, regression gate), §11.0 Agent vs Planner relationship + §11.1 **agent loop sequence diagram** (mermaid `sequenceDiagram`: probe→replan→confirm→execute) + §11.2 agent caps table (`maxProbeSteps=3`/`maxWallClockMs=30000`/`maxReplanRounds=2`/cancel-always-wins) + §11.3 read-only probe strategy (auto-run `sideEffectClass=read` only; 4-class table) + §11.4 **`AgentBridge` interface + `AgentTurnResult` sealed class** (PlanReady/Probing/Clarify/Refuse/Done — P2 seam), §12.0 **`SttProvider` interface for the first time** + §12.1 voice→planner path normalization + §12.2 partial hypothesis rule (isFinal-gated, no mid-utterance compile) + §12.3 voice coreference resolution via Memory, §13.0 on-device characteristics table + §13.1 routing strategy (5-class: simple→on-device/complex→cloud/privacy→on-device-preferred/CLI→parser/FAQ→recipe) + §13.2 fallback chain (on-device→cloud opt-in, privacy-sensitive no auto-fallback), §14.1 **prompt-injection marking protocol** (resolving 08 §11's "marked untrusted" with no format — normative `{untrusted:true, source, text}` JSON + detection rule for post-untrusted high-risk command → force Clarify) + §14.2 social engineering defense + §14.3 data exfiltration defense (`{{secret.<key>}}` templates, no secrets in IR args/log), §15.0 observability fields table (12 fields) + §15.1 **planner performance budgets** (cloud p95≤3000ms/on-device≤800ms, token caps prompt≤7000/completion≤2000, repair backoff) + §15.2 telemetry privacy (allowlist/blocklist), §16.0 **golden fixture format** (JSON schema with expectedIr/registryFixture/memoryFixture) + §16.1 evaluation metrics table (8 metrics, 2 safety-critical) + §16.2 regression testing CI gate, §17 MVP vs V1 expanded to P1/P2/P3 3-column table (15 rows), §18.1 testing matrix (11 test classes). **CompileResult structured Clarify/Refuse synced to 03 §14.1** (EN+ZH): `Clarify` gained `options: List<ClarifyOption>?` + `slots: List<ClarifySlot>?`; `Refuse` gained `category: RefuseCategory` + `suggestions: List<String>?`; new `ClarifyOption`/`ClarifySlot`/`RefuseCategory{POLICY,IMPOSSIBLE,QUOTA,CAPABILITY}` types — aligning MCOS with mainstream AI-agent practice so the UI can render option cards and classify refusals. **10 gaps resolved:** all key types undefined (15 types defined in §3.0), Planner/Agent ambiguity (split into two concepts with distinct interfaces), tool-call mode unspecified (native-preferred + freeform-fallback + constrained-V2+ adapter decision rule), structured Clarify/Refuse (aligned with mainstream agents, synced to 03), repair loop had no state machine (mermaid stateDiagram added), agent loop was ASCII sketch (mermaid sequenceDiagram + AgentBridge interface), STT provider undefined (interface + partial-hypothesis rule), prompt-injection marking format missing (normative JSON protocol), no planner performance budgets (cloud/on-device latency + token caps), no evaluation framework (golden fixture format + metrics + regression gate). No content in 01/02/05/07/08/09 was modified; 03 §14.1 received only the structured Clarify/Refuse/ClarifyOption/ClarifySlot/RefuseCategory additions (EN+ZH).

### Docs — Memory expansion (merged into 07-memory.md) + 06 cross-ref fix
- Expanded [docs/en/07-memory.md](./docs/en/07-memory.md) (+ Chinese mirror) from a ~340-line sketch (15 sections, all illustrative, **7 key types undefined** — `WritePolicy`/`MemoryFilter`/`MemoryHit`/`MemoryRef`/`ResolveResult`/`MemoryExport`/`ImportMode` referenced in `MemoryFacade` but never defined) into a ~1121-line **implementable Memory subsystem design** organized around the core design driver: **maximize context reuse, minimize token consumption**. Strategy: **cross-reference rather than duplicate** — types already normative in [01](./docs/en/01-architecture.md) (Memory subsystem §6.3, `HostServices.memory()` §11.1, `ExecutionContext.memoryDefault()` §11.2, Stage 4 Expand §9.2), [02](./docs/en/02-command-protocol.md) (`x-mcos-ref`/`x-mcos-default-from-memory` §5.3, ref resolution order §5.4, `ref_unresolvable` error), [03](./docs/en/03-runtime.md) (Memory Facade §12, Audit Log §13, `PlannerBridge` §14, `CompileResult` §14.1), [04](./docs/en/04-plugin-sdk.md) (`MemoryFacade` read-only plugin view §6.6, `SecureStore` §6.4, `x-mcos-ref` author guide §4.5, `FakeRuntime` §14.1), [05](./docs/en/05-workflow.md) (`$memory` event filter §9.2, `__memory.*` `$ref` §6.0/§6.4, `fromMemory` recipe placeholder §14.1, error decision tree §7.0), [06](./docs/en/06-agent.md) (`PlannerContext.memorySnippet` §3.0, catalog retrieval §4.1, system prompt §9.0, prompt-injection marking §14.1, episodic first-use check §8.1, telemetry privacy §15.2), [08](./docs/en/08-security.md) (secrets §9, prompt injection §11, network egress §12, enterprise §13, non-goals §16), and [10](./docs/en/10-roadmap.md)/[11](./docs/en/11-implementation-status.md) (P2 phasing) are compressed to pointers; the new material is the Memory-internal detail that was missing or only sketched. Key additions: §1 reframed Memory as the **primary token-reduction lever** (not just convenience); §2 added "Token-efficient" as a design goal; §3 **rewrote the 5-layer sketch into the MemGPT/Letta three-tier model** (Core/Recall/Archival — Core pinned ~600-2000 tokens always in context, Recall rolling log compacted at 8K threshold, Archival local vector index top-K(5) on-demand; mermaid `flowchart` showing paging relationships; explicit token-cost-per-turn table; Working Memory kept separate as Workflow-Engine-owned); §4.0 **normative Kotlin types for the first time** (`Profile`/`Place`/`Person`/`Device` data classes); §4.1–4.4 full field tables (Type/Required/Default/Constraint) replacing JSON-only examples; §4.5 **per-field `syncable` vs `local_only` marking** (location coords default `local_only`); §5.0 **all 7 undefined types defined for the first time** (`WritePolicy`/`MemoryFilter`/`MemoryLayer`/`MemoryHit`/`MemoryRef`/`ResolveResult` sealed class with `Resolved`/`Ambiguous`/`NotFound`/`MemoryExport`/`ImportMode`/`MemoryWriteResult`/`WriteStatus`/`ConflictInfo`/`MemoryCategory`/`ImportResult`) + **method-name alignment** (`resolve(ref)` → `resolveRef(ref)` to match 03/02/04); §5.1 WritePolicy + provenance recording; §5.2 **conflict resolution** (soft-delete + timestamped provenance = lossless; write-time embedding similarity conflict detection; category-based confirmation policy table: PREFERENCE silent overwrite / PAYMENT+PERMISSION force Clarify; Phase 3 vector clock reserved); §6.0 **normative `resolveRef` algorithm** (5-step: exact label → alias → fuzzy/embedding → ambiguous Δsim<0.05 → not-found, with confidence scoring); §6.1 semantic types table (device/place/person/wifi/contact/room); §7.0 "Remember" trigger rules (USER_EXPLICIT/CONFIRMED_SUGGESTION/SYSTEM) + §7.1 **Planner proposal approval flow** (Clarify → user confirms → put with `confirmationId`, anti-pattern: silent put forbidden); §8.0 normative `EpisodicRecord` data class + §8.1 **time-decay retrieval** (7d=1.0/30d=0.5/90d=0.2/>90d=0.05) + §8.2 retention policy + §8.3 uses table; §9 **rewrote Semantic Index as 3-logical-index + hybrid retrieval + RRF** (one SQLite physical store, 3 logical indices: commands=BM25+dense / facts=dense / episodes=dense+time-decay; **RRF merge algorithm** normative pseudocode k=60; rerank strategy table; index refresh timing table; cloud embedding minimization) + §9.6 **token savings quantified** (no-Memory vs full-injection ~5-10K vs retrieval-snippet ~1000 vs on-device-paging ~600); §10.0 storage engines table + §10.1 AES-256-GCM encryption spec + §10.2 index size estimation (~3-12MB on-device); §11 **sync with vector clock** (§11.1 `(deviceId, lamportClock)` LWW + concurrent-conflict-to-user, CRDTs explicitly rejected as overkill) + §11.2 per-field syncable + §11.3 enterprise policy; §12.0 user settings table (7 settings) + §12.1 enterprise controls + §12.2 **Memory audit / provenance inspection** (every put records policy/writerId/timestamp/supersededPath, user-inspectable trail); §13.0 **four memory binding mechanisms disambiguation table** (`$memory` event filter / `__memory.*` `$ref` / `x-mcos-ref` / `fromMemory` — resolving the gap where 05 §6.4 only covered 3 of 4) + §13.1 normative `$memory` event filter (`resolveMemory: "arm"|"fire"`) + §13.2 `__memory.*` `$ref` + §13.3 error behavior table; §14 **Snippet Assembly (Normative)** — the core token-reduction section: §14.0 assembly algorithm (base window ~600 tokens + retrieval supplement ~400 tokens, ≤1000 total, normative pseudocode) + §14.1 **base window selection** (top-5 recently-used places/people/devices from episodic `entities`, not alphabetical) + §14.2 retrieval token truncation (no partial-entry truncation) + §14.3 **prompt-cache prefix ordering** (stable prefix `[system][core memory base][tools]` cached → volatile suffix `[retrieval][history][message]`; ~90% discount on cached prefix) + §14.4 snippet injection into system prompt §3 + §14.5 untrusted entry marking; §15 **Cross-Turn Reuse & Compaction** — the second core token-reduction section: §15.0 **5-strategy layered context management table** (re-send tiny Core / prompt-cache prefix / early local compaction at 8K / structured `session_state` / on-device MemGPT paging) + §15.1 **local compaction algorithm** (normative pseudocode: >8K → summarize old + keep last 5 verbatim + archive original to episodic lossless) + §15.2 **structured `session_state`** blob (`currentWorkflowStep`/`pendingPermissions`/`lastCommand`/`lastCommandResult`/`resolvedRefs` — ~50-100 tokens, survives compaction, mirrors Anthropic's NOTES.md pattern) + §15.3 **on-device MemGPT paging** (P3 differentiator: fixed-offset Core + archival paged via `memory_lookup` tool call = 0 retrieval tokens, impossible with cloud providers) + §15.4 compaction irreversibility (lossy in-session but lossless in episodic backup + `session_state` survives); §16 MVP/V1 expanded to P1/P2/P3 3-column table (18 rows); §17 testing matrix (9 test classes). **Design decisions grounded in mainstream AI agent research**: MemGPT/Letta three-tier hierarchy (core pinned + archival top-K), Claude Code compaction (8K threshold, keep last 5, root CLAUDE.md survives), Anthropic context engineering (context rot, structured note-taking), Pinecone hybrid retrieval (BM25+dense for keyword-like command names), ChatGPT memory (model-judged overwrite + user audit). **06 §8.1 broken cross-reference fixed** (EN+ZH): `[07 §4]` (Profile Schema) → `[07 §8]` (Episodic Memory) for the "first-use episodic check" — §4 is Profile Schema, not episodic. No content in 01/02/03/04/05/06/08/09/10/11 was modified except the 06 §8.1 one-line cross-ref fix.

### Docs — Security & Permission Model expansion (merged into 08-security.md)
- Expanded [docs/en/08-security.md](./docs/en/08-security.md) (+ Chinese mirror) from a ~273-line sketch (18 sections, all bullet/table-only, **key types undefined** — `ConfirmationPrompt` referenced in 01 §11.5 but never defined anywhere in the doc set; `PermissionScope`/`GrantRecord`/`GrantState`/`TrustLevel`/`EnterprisePolicy`/`SideEffectClass` all referenced by 01/03/04/06/07 but never normatively defined; §4 side-effect matrix a static 5×3 table with no decision algorithm; §11 prompt injection 3 rules with no threat classification or detection chain; §12 network egress 3 bullets with no decision algorithm; §13 enterprise policy a single JSON example with no type or merge semantics) into a ~1412-line **implementable Permission Kernel + security subsystem design** by inserting 18 new subsections in place (per the one-doc-per-topic convention, keeping existing section numbers §1–§19 so the 20+ existing cross-references from other docs all continue to resolve). Strategy: **cross-reference rather than duplicate** — types already normative in [01](./docs/en/01-architecture.md) (`CommandDescriptor` schema §10, `sideEffectClass` 5-value enum §10.1, Grant `scope` vocabulary §10.2, `AuthStamp` data class §11.4, `RuntimeEvent.ConfirmationNeeded` §11.5, `Source` enum §11.6, 10-stage pipeline Stage 6 Authorize §9.2), [02](./docs/en/02-command-protocol.md) (`x-mcos-secret` field marker §5.3), [03](./docs/en/03-runtime.md) (Permission Kernel §7, Audit Log §13 + redaction walk §13.3 + retention + HMAC export, ConfigChanged §19, plugin unload §6, cancellation §9.4, Scheduler queues §8), [04](./docs/en/04-plugin-sdk.md) (`HostServices` facade §6, `SecureStore` interface §6.4, `MemoryFacade` read-only §6.6, `http` object §11.1, `mcos-sdk-gradle` checker §13.2, `FakeRuntime`/`FakePermissionKernel` §14.1, error codes §8.1, prompt-injection checklist §13), [05](./docs/en/05-workflow.md) (confirm step §5.7, `ConfirmationPolicy` §10, pre-auth recipes §10), [06](./docs/en/06-agent.md) (`ConfirmationPrompt` rendering §8.1, system prompt safety rules §9.0, prompt-injection marking format §14.1, social engineering defense §14.2, `Refuse` categories §5.5, on-device routing §13, telemetry privacy §15.2, STT §12), and [07](./docs/en/07-memory.md) (episodic first-use §8, snippet untrusted marking §14.5, sync §11, enterprise §13, non-goals §16) are compressed to pointers; 08 is the normative owner of the security-specific types and algorithms that were missing. Key additions: §1.1 **STRIDE mapping table** (6 classes × MCOS manifestation × primary mitigation) + §1.2 **trust-boundary mermaid flowchart** (User→Planner(untrusted)→Compiler→Runtime→Plugin→OS, 4 boundaries each with a distinct gate); §2.1 **per-layer failure-mode table** (if bypassed → backup layer) + §2.2 **fail-closed principle** (cannot-read-grant-store → DENY not allow; policy-parse-fail → restricted mode; sideEffectClass-undetermined → treat as destructive; confirmation-timeout → DENY); §3.0 **normative `PermissionScope` sealed class** (7 variants: `PluginExecute`/`Command`/`EventSubscribe`/`Memory`/`Network`/`McpServer`/`Android`) + normative ABNF grammar for scope strings + parsing rule (unknown prefixes rejected, not wildcarded) + §3.4 **AND-semantics scope combination rule** (command execution requires all scopes simultaneously satisfied; broad grants do not substitute for specific ones — prevents confused-deputy escalation); §4.0 **normative `decideConfirmation()` decision algorithm** (pure function, 6-step: enterprise force-check → sticky-deny → base matrix `sideEffectClass × grantState` → background-event stricter → user global tightening → first-use awareness; outputs `ConfirmAction` enum: ALLOW/CONFIRM_ONCE/CONFIRM_SESSION/CONFIRM_ALWAYS/DENY; 5 key invariants: DESTRUCTIVE always CONFIRM_ONCE, enterprise only tightens, sticky denial absolute, background events stricter, user can only tighten) + §4.2 user global tightening table (4 settings) + §4.3 enterprise force-confirm override semantics + §4.4 **sideEffectClass honesty check** (4-layer: gradle static analysis / marketplace CI / runtime instrumentation V1+ / audit post-hoc); §5.0 **normative `GrantRecord` data class + `GrantState` enum** (8 states: NOT_REQUESTED/ASKED/GRANTED/DENIED/ASK_ALWAYS/FOREGROUND_ONLY/EXPIRED/REVOKED) + `GrantScope` enum + §5.1 **grant lifecycle state machine** (mermaid `stateDiagram-v2`) + §5.2 **`AuthStamp` lifecycle rules** (minted at Stage 6, attached to ExecutionContext, verified by HostServices facades, expired on run completion, non-forgeable HMAC signature) + §5.3 sticky denial semantics (terminal within session, only Settings reset) + §5.4 grant cache warm-start algorithm; §6.0 **normative `ConfirmationPrompt` data class** (filling the 01 §11.5 reference gap — 10 fields including `irPreview`/`riskBadge`/`typedAckRequired`/`destinationUrl`/`timeoutMs`) + `RiskBadge` enum + `ConfirmOption` enum + `SideEffectClass` enum + **options-population rule table** (DESTRUCTIVE never offers ALLOW_PERSISTENT) + riskBadge derivation algorithm + §6.1 rendering requirements table (MUST/SHOULD/MAY) + §6.2 destructive typed-acknowledgment (GitHub-style "type DELETE" V1) + §6.3 background event confirmation escalation (notification → foreground confirm → no silent execution) + §6.4 confirmation timeout (DENY but not sticky; triple-timeout → sticky); §7.0 **normative `TrustLevel` enum** + §7.2 trust-level→isolation mapping table + §7.3 trust-level change triggers (marketplace review → upgrade; security incident → downgrade to UNTRUSTED); §8.1 **process isolation boundary mermaid flowchart** (main process + per-plugin bound services, Binder IPC) + §8.2 Binder identity checks (caller UID + AuthStamp + scope match) + §8.3 scoped HostServices (no raw Context; 6 narrow facades listed; forbidden methods enumerated) + §8.4 Accessibility module dedicated isolation; §9.1 SecureStore namespace isolation (per-plugin Keystore key alias, cross-plugin read impossible, Runtime namespace inaccessible) + §9.2 **`{{secret.<key>}}` template resolution algorithm** (4-step: manifest declares → Planner sees template only → Stage 4 Expand resolves → audit records template form; Planner never sees secret values = data-leak prevention) + §9.3 secret rotation/revocation table + §9.4 audit redaction policy table (5 field markers); §10.1 normative rate-limit parameters table (7 limits) + §10.2 **exponential backoff algorithm** (normative pseudocode, cap 30s, per-(commandId,source)) + §10.3 rate-limit error code table; §11.0 **prompt-injection threat classification table** (4 classes: instruction override / privilege escalation / data exfiltration / social engineering × detection point × mitigation) + §11.1 **untrusted-content marking rule** (normative owner: 6 sources that MUST mark, propagation rule through Memory archival, `x-mcos-untrusted` schema marker; format cross-references 06 §14.1) + §11.2 **permission non-expansion rule** (authorization system is text-blind — Stage 6 never parses IR text for permissions; grants only created by user action) + §11.3 **detection chain** (normative 5-step: read untrusted → emit IR → compiler checks new-high-risk → force Clarify → prompt includes untrusted source) + §11.4 **owner-relationship clarification table** (6 concerns × owner × location: 08=rules, 06=format, 07=application); §12.0 **normative `decideEgress()` decision algorithm** (4-step: kill switch → HTTPS enforcement → scope glob check → enterprise allow/deny; pure function; `EgressDecision` sealed class) + §12.1 **domain matching rules** (glob table + normative `globMatch` algorithm + IDN/punycode normalization) + §12.2 confirmation URL display rules + §12.3 MCP/cloud-LLM independent toggles table + §12.4 global kill switch behavior (fail-closed, not overridable, immediately cancels in-flight); §13.1 **normative `EnterprisePolicy` data class** (12 fields) + §13.2 policy semantics table (9 fields × semantics × user-setting interaction) + §13.3 **delivery & fail-closed algorithm** (normative 5-step: fetch → parse → on-fail enter FAIL_CLOSED mode with hardcoded safe-set → on-fetch-fail use cache → emit ConfigChanged) + §13.4 **enterprise & user policy merge rule** (most-restrictive-wins table, 6 dimensions; formal: `result = max(enterprise, user) restrictiveness`); §14.1 **complete security audit event table** (13 event types × trigger × key fields) + §14.2 relationship to audit schema (security events in same `steps_json`, not separate stream) + §14.3 remote attestation (future V2+, Merkle root + hardware attestation); §15.1 **signed-manifest verification algorithm** (6-step: lookup key → verify signature → no downgrade → no publisher change → SDK check → atomic swap) + §15.2 min/max SDK validation + §15.3 **crash-loop quarantine algorithm** (3 crashes in 60s → quarantine + rollback or disable); §17 MVP/V1 expanded to P1/P2/P3 3-column table (16 rows); §18 **testing matrix** (7 test classes: Permission Kernel decision tests 13 cases / ConfirmationPrompt rendering 6 cases / prompt-injection detection 5 cases / egress decision 9 cases / enterprise policy merge 5 cases / rate-limit & backoff 4 cases + `FakePermissionKernel` usage example showing it implements the real algorithm not a stub). **5 gaps resolved:** `ConfirmationPrompt` undefined (01 §11.5 reference filled — 10-field data class), `PermissionScope`/`GrantRecord`/`GrantState`/`TrustLevel`/`EnterprisePolicy` undefined (normative Kotlin types defined), side-effect policy was static table (normative `decideConfirmation` algorithm with 6-step logic), prompt-injection was 3 rules (threat classification + detection chain + permission-non-expansion invariant), network egress was 3 bullets (normative `decideEgress` algorithm + glob matching + kill switch). No content in 01/02/03/04/05/06/07/09/10/11 was modified; all 20+ existing cross-references to 08 continue to resolve (section numbers preserved).

### Docs — Marketplace expansion (merged into 09-marketplace.md)
- Expanded [docs/en/09-marketplace.md](./docs/en/09-marketplace.md) (+ Chinese mirror) from a ~273-line sketch (16 sections, all table/bullet-only, **marketplace-specific types undefined** — `PackageMetadata` had a JSON example but no type definition; `PublisherKey`/`SigningResult` had a 4-step description but no types; `ReviewState` had a mermaid sequence but no state enum; `InstallState` had pseudocode but no state enum; `BlocklistEntry`/`Blocklist` had bullets but no types; §5 CI gates described as "marketplace CI gate ([09 §5.1])" from 04 §13.2 but the 7 gates were not listed in 09; §6 signing had no key lifecycle or transparency log; §7 install had no permission diff algorithm; §9 search ranking was 3 bullets with no algorithm; §11 API was 8 lines with no endpoint table; §14 takedown had no signed blocklist; §13 private registry had no config type; §12 monetization was non-normative bullets) into a ~1493-line **implementable marketplace subsystem design** by inserting 17 new subsections in place (per the one-doc-per-topic convention, keeping existing section numbers §1–§17 so the 16+ existing cross-references from 03/04/05/08 all continue to resolve). Strategy: **cross-reference rather than duplicate** — schema already normative in [05](./docs/en/05-workflow.md) (recipe envelope schema §14.1 — recipeId/name/version/workflow/placeholders/requiredPlugins/triggerPreview + field table + security constraints), checker already normative in [04](./docs/en/04-plugin-sdk.md) (`mcos-sdk-gradle` §13.2 — 7 check items), runtime signature verification cache in [03](./docs/en/03-runtime.md) (§16.2 key-id + plugin hash → verified-at), TrustLevel enum in [08](./docs/en/08-security.md) (§7 — 5-value enum), `PermissionScope` sealed class in [08](./docs/en/08-security.md) (§3.0 — 7 variants), `EnterprisePolicy` in [08](./docs/en/08-security.md) (§13.1 — 12 fields), `CommandDescriptor` in [01](./docs/en/01-architecture.md) (§10), MCP conversion in [02](./docs/en/02-command-protocol.md) (§12.4), and Memory binding in [07](./docs/en/07-memory.md) (`fromMemory` §14.1) are compressed to pointers; 09 is the normative owner of the marketplace-specific types and algorithms that were missing. Key additions: §1.1 **marketplace phasing table** (P1 sideload only / P2 built-in plugins distributed via marketplace protocol / P3 public index with publisher self-service — aligning with [11 §5](./docs/en/11-implementation-status.md) P3 = Ecosystem) + §2.1 **design trade-off table** (safety vs growth hacking: 6 tensions × resolution); §3.1 **publishing flow mermaid `sequenceDiagram`** (Publisher → SDK validator → CI pipeline → human review → CDN → client push notification, 7 participants); §4.0 **normative `PackageMetadata` data class** (18 fields including `permissionsPreview`/`commandsPreview`/`artifact`/`safetyScore`) + `PermissionEntry` + `ArtifactRef` types + §4.2 `permissionsPreview` spec (cross-refs 08 §3.0 `PermissionScope` + 08 §7 `TrustLevel` risk tiers) + §4.3 `commandsPreview` spec (cross-refs 01 §10 `CommandDescriptor`) + §4.4 **metadata cache strategy** (offline-friendly, TTL + ETag conditional fetch, CDN edge cache); §5.0 **normative `ReviewState` enum** (9 states: SUBMITTED/CI_CHECKING/CI_REJECTED/HUMAN_REVIEW/APPROVED/REJECTED/LISTED/UNLISTED/REVOKED) + **review state machine** (mermaid `stateDiagram-v2`) + §5.1 **complete CI gate list** (11 gates: mirrors 04 §13.2's 7 `mcos-sdk-gradle` checks + 4 marketplace-specific — malware scan, signature verification, namespace arbitration, manifest schema) + §5.2 **human review trigger table** (6 triggers × severity × SLA: new publisher / high-permission / egress / payment / sensitive namespace / flagged) + §5.3 review SLA (auto-CI p95 < 5 min; human review p95 < 5 business days) + §5.4 **rejection feedback format** (JSON report isomorphic with `mcos-sdk-gradle` output so authors can fix locally then resubmit); §6.0 **normative `PublisherKey` data class** (6 fields: keyId/publisherId/publicKeyFingerprint/algorithm/createdAt/rotatedFrom/status) + `KeyStatus` enum + `SigningResult` type + §6.1 **publisher key registration flow** (generate Ed25519 keypair → upload public key fingerprint → private key stored in HSM or local encrypted keystore; ECDSA not supported, RSA-PSS-4096 legacy) + §6.2 **signature verification algorithm** (client-side, cross-refs 03 §16.2 cache; 4-step: fetch metadata → verify sha256 → verify publisher signature → verify marketplace signature; fail-closed on any failure) + §6.3 **key rotation & revocation** (compromised key → all versions signed by that key pushed to global blocklist → client force-disables on next sync) + §6.4 **transparency log** (V1+, append-only Merkle tree like Certificate Transparency, third-party auditable, guards against retroactive package tampering) + §6.5 **TrustLevel mapping** (verification outcome → 08 §7 TrustLevel: marketplace-reviewed → MARKETPLACE_VERIFIED; sideloaded → UNVERIFIED; blocklisted → UNTRUSTED); §7.0 **normative `InstallState` enum** (10 states: NOT_INSTALLED/DOWNLOADING/VERIFYING/STAGING/LOADING/INSTALLED/UPDATE_AVAILABLE/DISABLED/UNINSTALLING/FAILED) + **install state machine** (mermaid `stateDiagram-v2`) + §7.1 **complete install flow** (normative algorithm: fetch metadata → display permission preview → user consent → download → sha256+signature verify → stage → load → grant defaults; cross-refs 08 §4.0 `decideConfirmation` for per-command confirmation at runtime, NOT at install time) + §7.2 **permission diff algorithm** (normative: `PermissionDiff` data class with added/removed/changed lists; `consentRequired` = true if any added permission is risk tier ≥ MEDIUM or any change escalates risk tier; `ChangeType` enum: RISK_TIER_ESCALATED/JUSTIFICATION_CHANGED) + §7.3 **uninstall flow** (cross-refs 03 §6 drain mechanics + 08 §5 grant revocation + SecureStore key cleanup) + §7.4 **dependency resolution** (recipe install: `requiredPlugins` SemVer range parsing, SAT solver V1+, simple highest-match P3); §8.0 **recipe store cross-reference** (05 §14.1 owns the envelope schema — recipeId/name/version/workflow/placeholders/requiredPlugins/triggerPreview; 09 owns marketplace publishing/search/wizard semantics) + §8.1 recipe publishing flow + §8.2 recipe search (by command, by trigger type, by required plugin) + §8.3 **install wizard** (placeholder binding: `fromMemory` suggests from 07 Memory, `default` fallback, `required` blocks skip; bound values stored in Memory, substituted at compile time — cross-refs 05 §14.1 + 07 `fromMemory`); §9.0 search mechanism table + §9.1 **ranking algorithm** (normative: `score = textScore × 0.3 + categoryMatch × 0.2 + popularity × 0.2 + safetyScore × 0.3`; safetyWeight ensures high-permission plugins are ranked lower but not hidden — floor 0.3 so defense-in-depth doesn't create a discoverability black hole; permission safety weight dampens but never fully suppresses) + §9.2 **recommendation strategy** ("Commands used by you" algorithm: extract missing command IDs from user's command history → recommend plugins that provide those commands) + §9.3 offline cache (metadata browsable without network, install deferred); §10.0 MCP catalog bridge + §10.1 **MCP template publishing** (config stub + namespace suggestion: `mcp.<server>.<tool>` mapped to MCOS command ID) + §10.2 cross-refs 02 §12.4 conversion table + 04 §10 MCP Adapter; §11.0 **normative REST endpoint table** (17 endpoints: search, package detail, download, publish, review status, blocklist fetch, recipe search, recipe detail, telemetry, publisher key register, publisher key rotate, version list, category list, featured, report, uninstall telemetry, signing key transparency log) × (method + path + auth + request fields + response fields) + §11.1 **search parameter spec** (query/category/sort/pagination/cursor) + §11.2 **publish endpoint** (publisher bearer auth + multipart upload: manifest + artifact + signature) + §11.3 **telemetry endpoint** (opt-in install events, cross-refs 06 §15.2 privacy rules — no PII, anonymized device class only) + §11.4 **error response format** (aligns with MCOS `McosErrorCode` enum: `PACKAGE_NOT_FOUND`/`SIGNATURE_INVALID`/`VERSION_CONFLICT`/`PERMISSION_DENIED`/`RATE_LIMITED`/`BLOCKLISTED`) + §11.5 OpenAPI spec location (`mcos-server/api/openapi.yaml`); §12.0 monetization (non-normative, deferred to business decision) + §12.1 **model trade-off table** (free / paid per-install / subscription / enterprise private registry — 4 models × revenue potential × friction × ecosystem impact); §13.0 private/enterprise registry + §13.1 **`RegistryConfig` data class** (6 fields: baseUrl/caPin/requiresVpn/allowPublicIndex/authType/priority) + `RegistryAuth` enum (NONE/BEARER/CLIENT_CERT/OAUTH) + §13.2 enterprise integration (cross-refs 08 §13.1 `EnterprisePolicy`: marketplace URL pin, allowed publisher list, required categories) + §13.3 **public/private coexistence** (client queries multiple registries by priority; private wins on package ID collision; blocklist from public registry still applies to private); §14.0 **normative `BlocklistEntry` data class** (6 fields: packageId/versionRange/reason/detailUrl/blockedAt/expiresAt) + `BlocklistReason` enum (6 values: MALWARE/SIGNATURE_KEY_COMPROMISED/POLICY_VIOLABILITY/PUBLISHER_BANNED/SECURITY_VULNERABILITY/LEGAL_TAKEDOWN) + `Blocklist` data class (entries + version + issuedAt + **signature** — the marketplace signs the blocklist itself to prevent injection attacks; fail-closed: over-blocking is safer than under-blocking) + §14.1 user report flow + §14.2 publisher suspension flow (freeze publishing + audit existing packages) + §14.3 **blocklist distribution** (client fetches at startup + periodic poll; signed with marketplace key; fail-closed if signature invalid — treat as "no blocklist update" rather than "accept unsigned") + §14.4 **force-disable** (already-installed blocklisted plugin: cross-refs 03 §6.5 unload + 08 §7.3 trust downgrade to UNTRUSTED; user notified, plugin disabled, data preserved for appeal); §15 **P1/P2/P3 3-column phasing table** (aligned with 11 §5: P1 sideload / P2 built-in + private registry / P3 public index + recipes + MCP bridge + transparency log); §16 **testing matrix** (6 test classes: CI gate tests 11 cases / signature verification 8 cases / install-update-uninstall state machine 12 cases / permission diff 7 cases / blocklist distribution 6 cases / search ranking 5 cases). **Key design decisions:** (1) **install consent ≠ runtime grant** — installing a plugin does NOT pre-grant permissions; each command still goes through 08 §4.0 `decideConfirmation` at Stage 6; (2) **defense-in-depth search ranking** — permission safety weight dampens but never fully hides high-permission plugins (floor 0.3) so users can still find them if they search explicitly; (3) **signed blocklist** — the blocklist is itself signed by the marketplace to prevent injection attacks, and clients fail-closed on invalid signatures; (4) **Ed25519 preferred** for publisher signing keys (compact, fast verification on mobile), RSA-PSS-4096 legacy support, ECDSA explicitly not supported; (5) **05 §14.1 owns the recipe schema** — 09 §8 cross-references rather than redefines, owning only marketplace-specific publishing/search/wizard semantics; (6) **04 §13.2 owns the checker** — 09 §5.1 mirrors the 7 `mcos-sdk-gradle` gates + adds 4 marketplace-specific (malware scan, signature verification, namespace arbitration, manifest schema). No content in 01/02/03/04/05/06/07/08/10/11 was modified; all 16+ existing cross-references to 09 continue to resolve (section numbers preserved).

### Docs — Roadmap expansion (merged into 10-roadmap.md)
- Expanded [docs/en/10-roadmap.md](./docs/en/10-roadmap.md) (+ Chinese mirror) from a ~344-line outline (14 sections + Document Index, well-structured at the top level but **phase internals all bullet-only sketches** — §4.1–§4.5, §5.1–§5.6, §6.1–§6.5 were pure bullet headlines with zero tables, zero code, zero quantified SLAs, zero cross-references into the spec docs they depend on; zero mermaid diagrams anywhere in the doc; cross-referencing was nearly absent with only 1 in-body doc link; §11 Success Metrics used the ambiguous label "V1 (end of P2/P3 bridge)" that conflicted with the rest of the doc set where P2=V1 and P3=V2; Document Index omitted 11-implementation-status.md; no success gates, no vertical-slice cadence, no dependency topology, no risk severity matrix) into a ~834-line **detailed project roadmap** by inserting 17 expansion points in place (per the one-doc-per-topic convention, keeping existing section numbers §1–§14 + adding §15 Cross-Reference Index + §16 Testing & Validation Strategy, so all 10 existing inbound cross-references from 00/05/07/09/11/README/REPOSITORIES continue to resolve). Strategy: **cross-reference rather than duplicate** — the per-subsystem phasing tables already normative in [06](./docs/en/06-agent.md) §17, [07](./docs/en/07-memory.md) §16, [08](./docs/en/08-security.md) §17, [09](./docs/en/09-marketplace.md) §15, and the aggregated Global Feature Matrix in [11](./docs/en/11-implementation-status.md) §5 are compressed to pointers; 10 is the rollup view that shows phase-level dependencies, milestone gantt charts, success gates, and cross-subsystem critical paths. Key additions: §1.1 **5 design principles table** (spec-first / vertical-slice-demoable / safety-floor-every-phase / protocol-before-platform / docs-follow-code — each with explanation + rationale) + §1.2 **dependency topology mermaid `flowchart TD`** (the "soul diagram": Parser→Registry→Executor→PermissionKernel→Audit→Plugins→App→Planner→Workflow→Memory→Marketplace DAG, color-coded by P1/P2/P3, showing which subsystems block which); §2.1 **terminology alignment declaration** (explicitly states P1=MVP, P2=V1, P3=V2 — matching 06/07/08/09/11; warns about the P1–P8 naming collision with 00-vision §4 design principles) + §2.2 **phase dependency mermaid `flowchart LR`** (P0→P1→P2→P3→P4 with dotted "early prep" edges) + §2.3 **Success Gates table** (5 phases × primary metric × target × measured-by — P1: 100% fixture pass / P2: NL accuracy ≥80% p85 / P3: ≥10 external plugins / P4: ≥1 OEM preload); §3.1 **P0 deliverable inventory table** (10 items × status × location) + §3.2 **P0→P1 transition gates** (3 gates: spec review / fixture CI / bilingual parity); §4.0 **P1 milestone mermaid `gantt`** (week-by-week, 4 sections: Infrastructure / Runtime Core / Plugins & App / AI, aligned with 11 §6 dev path) + §4.1 App deliverables table (5 items × spec ref × exit criteria, cross-refs 01 §7 + 04 §6) + §4.2 Runtime subsystem table (6 subsystems × spec ref × P1 scope × explicitly-NOT-in-P1, cross-refs 02/03/05/08) + §4.3.1 **command count budget** (20 commands broken down by plugin, justifying the "20+" success metric) + §4.4 Planner P1 scope table (5 capabilities × P1 scope × P2+ deferred, cross-refs 06 §17, explicitly states P1 = Planner only) + §4.5 Non-Goals with "why deferred" explanations + §4.6.1 **MVP Exit Criteria Checklist** (6 checkboxes: fixture pass / external plugin / DSL preview / permission flow / audit log / NL single-command); §5.0 **P2 milestone mermaid `gantt`** (month-by-month, 4 sections: Workflow / Planner / Memory / Plugins & Events) + §5.1–§5.5 all 5 Phase-2 subsystems converted from bullets to **feature delivery tables** (Feature × Spec ref × P2 scope × Deferred, each cross-referencing 05 §15 / 06 §17 / 07 §16 / 08 §17) + §5.5 Event Bus event table (3 events × trigger × workflow use) + §5.6.1 **P2 Exit Criteria Checklist** (6 checkboxes); §6.0 **P3 critical path** (marketplace signing → public index → third-party onboarding dependency chain) + §6.1–§6.3 all converted to feature delivery tables (cross-refs 09 §15 / 02 §12.4 / 04 §10 / 08 §17); §7.1 **trigger conditions table** (6 directional bets × trigger condition × dependency — e.g., multi-device needs P3 marketplace stable + ≥1 OEM interest); §8.1 **vertical slice demo cadence table** (7 sprints × slice × demo command × unblocks — from hello.world to marketplace install) + §8.2 **dependency unlock map** (10 steps from 11 §6 × what each unblocks, identifying parser as the single longest pole); §9.1 **phase staffing ramp table** (5 phases × team size × additions × rationale); §10.1 **risk severity × probability matrix** (6 risks × 2 dimensions → priority emoji 🔴🟡🟢) + §10.2 **3 additional risks** (LLM provider lock-in / on-device model fragmentation / regulatory AI Act/GDPR); §11.0 **terminology correction** (V1 definition changed from "end of P2/P3 bridge" to "P2 = V1, P3 = V2" — aligning with 06/07/08) + §11.1–§11.3 all 3 metric groups converted from bullets to **quantified metric tables** (Metric × Target × Measurement — 5+5+4 rows with specific numbers: p95 latency <500ms, cold start <2s, NL accuracy ≥80% p85, ≥10 plugins, <10min cold install); §12.1 **bilingual sync rules** (5 rules: EN leads / code identical / first-use glossary / CHANGELOG records / parity verified); §13.1 **acceptance criteria table** (5 actions × done-when condition); §15 **Cross-Reference Index** (16-row table mapping each roadmap section to its detailed spec location — the "jump table" for readers going from "what we're building" to "how it's specified"); §16 **Testing & Validation Strategy** (§16.0 golden fixture CI gate + §16.1 end-to-end demo validation per phase + §16.2 **performance regression gate table** with quantified targets: command p95 <500ms, cold start <2s, NL→IR p95 <3s, on-device <800ms, snippet assembly <100ms); Document Index **fixed** to include 11-implementation-status.md (was previously omitted). **3 key gaps resolved:** (1) **terminology inconsistency** — P1↔MVP↔V1 mapping now explicitly declared in §2.1, §11 corrected; (2) **zero diagrams** — 6 mermaid diagrams added (dependency topology flowchart + phase dependency flowchart + P1 gantt + P2 gantt); (3) **no quantified targets** — every phase now has specific success gates, exit criteria checklists, and performance regression thresholds. No content in 00/01/02/03/04/05/06/07/08/09/11 was modified; all 10 existing cross-references to 10 continue to resolve (section numbers preserved, §15/§16 are new additions at the end).

### Docs — Implementation Status sync (merged into 11-implementation-status.md)
- Updated [docs/en/11-implementation-status.md](./docs/en/11-implementation-status.md) (+ Chinese mirror) from 163 → 186 lines to sync with the expanded doc set (06–10 were all heavily expanded since 11 was last updated, introducing new subsystems, types, and phasing tables that 11 indexes). **5 gaps fixed:** (1) **§7 `PluginHost` → `HostServices`** (HIGH — the retired `PluginHost` name was still used in the SDK API Target contract summary that [04 §250](./docs/en/04-plugin-sdk.md) routes readers to; now correctly says `HostServices` with the `memory()` facade added + `McosException` error channel + `CommandResult.Err` carries `details: JsonObject` per [04 §5.2](./docs/en/04-plugin-sdk.md)/[02 §8.3](./docs/en/02-command-protocol.md); the historical `PluginHost` name is mentioned only in a "retired" deprecation note for searchability); (2) **§3 Subsystem Matrix +7 missing security subsystems** (HIGH — Network Egress `decideEgress` [08 §12](./docs/en/08-security.md), Prompt Injection Detection [08 §11](./docs/en/08-security.md), Rate Limiting [08 §10](./docs/en/08-security.md), Secret Management [08 §9](./docs/en/08-security.md), Crash-loop Quarantine [08 §15.3](./docs/en/08-security.md), Process Isolation [08 §8](./docs/en/08-security.md), Enterprise Policy [08 §13](./docs/en/08-security.md) — all have P1/P2/P3 phasing in 08 §17 but were absent from §3; also added §-level anchors to existing rows like Permission Kernel → `08 §4` and Audit → `08 §14`; added safety-floor blockquote noting P1 ships a complete minimal security story); (3) **§5 Global Feature Matrix overhauled** (MEDIUM — intro now cites the 5 per-doc phasing tables by anchor: 05 §15 / 06 §17 / 07 §16 / 08 §17 / 09 §15; security rows expanded from 2 to 6 to match 08 §17 granularity: split out ConfirmationPrompt / Network Egress / Prompt Injection / Rate Limiting / Enterprise Policy / Crash-loop Quarantine as separate rows; **Marketplace sideload corrected** from P1→P2 to match [09 §1.1](./docs/en/09-marketplace.md) which says P1 ships built-in plugins only; Plugins P1 cell now references the 20-command budget from [10 §4.3.1](./docs/en/10-roadmap.md); phase terminology note added: P1=MVP/P2=V1/P3=V2 per [10 §2.1](./docs/en/10-roadmap.md)); (4) **§2 Module Topology aligned with REPOSITORIES.md** (MEDIUM — split into "P1 first-party modules (6)" vs "Planned modules (4)" matching REPOSITORIES.md §2/§3 structure; added `sys.intent.start` to `mcos-plugin-system` per REPOSITORIES.md §91); (5) **§1 doc-count phrasing** (LOW — "00–10 + this file" → "00–11" aligning with the convention used in 10-roadmap.md and other docs). No content in 00/01/02/03/04/05/06/07/08/09/10 was modified; all 10+ existing cross-references to 11 continue to resolve (section numbers §0–§8 preserved).

### Docs — Cross-document consistency pass (13 issues fixed across EN + ZH)
- Fixed **13 cross-document inconsistencies and normative code defects** identified during a full doc-set review, across all affected English and Chinese mirror files (one pass, no structural reorganization). **A-class (spec-breaking / would mislead implementers):** (1) `McosErrorCode` enum in [01 §15.1](./docs/en/01-architecture.md) was invalid Kotlin — `COMPILE_FAILED(false);` had a semicolon that prematurely terminated the constant list before `WORKFLOW_INVALID`/`TRIGGER_MISFIRE`; merged all 17 constants into a single list with the mandatory separator only after `TRIGGER_MISFIRE(true);`. (2) `CONFLICT` was an **orphan error code** — used in [02 §8.3](./docs/en/02-command-protocol.md) (`details` schema) and [03 §8.5](./docs/en/03-runtime.md) (deadlock detection) but absent from the enum and the [02 §10.3](./docs/en/02-command-protocol.md) standard codes table; added to both (01 §15.1 enum + §15.2 reference table, 02 §10.3), with meaning "resource contention: per-device serial lock held or duplicate manifest ID". (3) `UNAUTHORIZED` at [02 §9.6](./docs/en/02-command-protocol.md) retry-cleanliness table → `PERMISSION_DENIED` (the correct enum member). (4) `INVALID_ARGUMENT` / `INTERNAL_ERROR` at [04 §11.1](./docs/en/04-plugin-sdk.md) `errorMapping` defaults + [09](./docs/en/09-marketplace.md) SemVer range error + REST error table → `SCHEMA_VIOLATION` / `INTERNAL` (the only codes that exist in the enum); also clarified [09 §11.4](./docs/en/09-marketplace.md) that the marketplace REST API uses its own HTTP-layer code vocabulary distinct from the DSL command `McosErrorCode` enum, so `UNAUTHENTICATED`/`NOT_FOUND`/`ALREADY_EXISTS` are intentionally separate. (5) Reserved namespace self-contradiction — [02 §4.3](./docs/en/02-command-protocol.md) defines the roots as `sys`/`mcp`/`mcos`/`std`, but CI checks in [04 §13.1/§13.2](./docs/en/04-plugin-sdk.md) and [09 §4.3/§5](./docs/en/09-marketplace.md) wrote `system.*` and omitted `std`; corrected all 4 locations to `sys.*` + added `std.*`. **B-class (type/example vs spec mismatch):** (6) `RuntimeConfig` in [03 §19](./docs/en/03-runtime.md) was missing the `networkAllowList` field referenced by the [04 §14.1](./docs/en/04-plugin-sdk.md) `FakeRuntime.Builder.config(...)` example; added `val networkAllowList: List<String> = emptyList()` with cross-ref to [08 §12](./docs/en/08-security.md) Network Egress Policy. (7) `RuntimeEvent.CommandStarted` in [04 §14.1](./docs/en/04-plugin-sdk.md) test assertions → `RuntimeEvent.RunStarted` / `RuntimeEvent.StepStarted` (the actual variants per [01 §11.5](./docs/en/01-architecture.md)). (8) DSL example `camera.scan format="qr"` at [04 §14.1](./docs/en/04-plugin-sdk.md) missing invocation parens → `camera.scan(format="qr")`. (9) `RuntimeFacade` interface in [01 §7.2](./docs/en/01-architecture.md) was missing `subscribe()` and `exportAudit()` methods that [03 §11.4](./docs/en/03-runtime.md) (EventBus cross-process registration) and [03 §16](./docs/en/03-runtime.md) (audit JSONL export) reference; added both with cross-ref comments. (10) `Trigger.Manual` in [05 §4.0](./docs/en/05-workflow.md) had three Kotlin compilation defects: `val as: String?` (`as` is a hard keyword → `val as_`), `val inputs: JsonObject = emptyMap` (missing parens + wrong type — the JSON example shows an array of parameter names → `val inputs: List<String> = emptyList()`). **C-class (metadata / hygiene):** (11) Date drift — [01](./docs/en/01-architecture.md) header `Last Updated: 2026-08-07` → `2026-08-06` (matching every other doc). (12) Broken cross-reference — [04 §13.1](./docs/en/04-plugin-sdk.md) "Namespace ownership" pointed to `[02 §4.3]` (Reserved Namespaces table) but the `namespaces[]` declaration + arbitration is in `[02 §4.4]`; corrected. (13) Fixture column off-by-one — `docs/fixtures/06-nested-call/expected.error.json` reported `column: 27` for `photo.compress()` in `mail.send(to="Tom", body=photo.compress())`, but the `p` of `photo` is at column 26 (verified by char-count); corrected to 26. **Structural note:** 01 §15.1 is reaffirmed as the single source of truth for `McosErrorCode`; 02 §10.3 and §8.3 reference it rather than maintaining independent lists.

### Docs — Cross-document consistency pass round 2 (A1–A6 + B1–B5, 11 issues fixed across EN + ZH)
- Fixed **11 deeper technical-design and contract-detail issues** identified in a second full doc-set review, across all affected English and Chinese mirror files. **A-class (technical-design gaps that would mislead implementers or over-promise security):** (A1) [08 §5.2](./docs/en/08-security.md) AuthStamp HMAC signature was written as a day-one defense, but MVP is single-process — in-process plugins share memory and could read the Keystore key / grant cache / forge a stamp; added ⚠️ MVP limitation blockquote clarifying it is a V1 boundary, MVP ships only `BUILTIN` plugins in production, and the stamp is a structural seam backed by static analysis + marketplace review until process isolation is active. (A2) [08 §12](./docs/en/08-security.md) network egress enforcement (`network.<domain>` scope, URL display, enterprise allowlist) was written as active, but in-process plugins can bypass `NetService` and use their own HTTP client in MVP; added ⚠️ MVP limitation blockquote clarifying `decideEgress()` is normative for V1+, MVP backstop is static analysis + marketplace review. (A3) **Planner↔Workflow binding syntax unification** — [06](./docs/en/06-agent.md) §5.2/§5.3 used a fabricated `{{steps[0].result.placeId}}` / `{{search.result.uris}}` syntax that exists nowhere in the normative grammar, while [05 §6.0](./docs/en/05-workflow.md) defines `$ref` + `__steps.<id>.value.<path>`; also [02 §7.2](./docs/en/02-command-protocol.md) Sequence IR had no binding field at all. Fixed by: rewriting all 06 Planner output examples (§5.2 sequence + §5.3 workflow + §16 golden fixture) to `$ref` object form for `args` binding; adding §7.2 "Output binding in sequences" declaration with `saveAs` + `$ref` example; fixing confirm step field name `message` → `prompt` (matching 05 §5.7); and noting `{{...}}` is only for prompt-string interpolation (not `args`). (A4) [08 §4.0](./docs/en/08-security.md) `decideConfirmation` step 6 depends on `isFirstUse` from episodic memory ([07 §8](./docs/en/07-memory.md)), which is P2 — but Permission Kernel is P1; added MVP fail-safe note: when episodic memory is unavailable, caller MUST pass `isFirstUse = true` (conservative). (A5) [01 §8](./docs/en/01-architecture.md) `DslParser` ran on `Dispatchers.Main.immediate` parsing untrusted input (Planner output / pasted DSL) — pathological nesting or oversized input could freeze the UI; moved to `Dispatchers.Default` and added [02 §6.10](./docs/en/02-command-protocol.md) "Input Size Bounds" table (token ≤4096, nesting ≤32, statements ≤64, bytes ≤256 KB, O(1) pre-check). (A6) [06 §13](./docs/en/06-agent.md) classified `mail.send` as `destructive` alongside `files.delete`, but email is network+write (leaves device, not delete/irreversible per [02 §8.1](./docs/en/02-command-protocol.md)); moved `mail.send` to `network` row. **B-class (contract-detail mismatches):** (B1) [08 §10.1](./docs/en/08-security.md) referenced `RuntimeConfig.rateLimits` / `RuntimeConfig.scheduler` as nested objects, but [03 §19](./docs/en/03-runtime.md) `RuntimeConfig` was flat with no such fields; added `RateLimits`, `SchedulerConfig`, and `UserPolicy` nested data classes to 03 §19 (defaults matching 08 §10.1/§4.2) and updated 08 §10.1 "Tunable via" column to reference specific field paths (`RuntimeConfig.rateLimits.maxInvokesPerMinute` etc.). (B2) Three confirmation-timeout defaults (30 s foreground / 5 min background / 120 s workflow confirm) were undocumented as a set; added [08 §6.4.1](./docs/en/08-security.md) "Confirmation Timeout Source Map" table unifying all three with rationale. (B3) [08 §8.2](./docs/en/08-security.md) `FileService.delete` "checks for a `destructive`-class grant" referenced a grant category that doesn't exist in the grant model (grants are per-command/per-scope, not per-class); rewritten to scope-based check (`stamp.grantsUsed` contains `command.files.delete`). (B4) [02 §10.3](./docs/en/02-command-protocol.md) standard error codes table is a subset of the full enum but wasn't labeled; added note pointing to [01 §15.1](./docs/en/01-architecture.md) as the complete enumeration. (B5) [02 §8.3](./docs/en/02-command-protocol.md) `CONFLICT` details schema only documented the duplicate-manifest shape (`resolvedId`/`requestedId`/`winningManifest`), not the device-deadlock shape (`heldDevice`/`requestedDevice`/`runId`) emitted by [03 §8.5](./docs/en/03-runtime.md); added `reason: "duplicate_id" | "device_locked"` discriminated union with both field sets.

### Docs — Strategic honesty + roadmap MCP spike/P3 split (round 6, V1/V2 + M1/M2/M3, EN + ZH)
- Two categories of change driven by a project-level (not just doc-level) review. **V-class (competitive-landscape honesty):** (V1) [00-vision.md §2.1](./docs/en/00-vision.md) claimed the "Mobile Command Bus" slot was empty (`∅`) — a factual error. Google (App Functions + Gemini) and Apple (App Intents + Apple Intelligence) are building OS-level command buses with structural advantages (pre-install, OS-level permissions, mandated app-vendor cooperation). Apple appeared in the repo only as design *inspiration* (06/07 §8), never as a competitor. Fixed by replacing the `∅` row with two rows: "Mobile command (OS-integrated)" listing Google/Apple (locked into their own ecosystems) vs "Mobile command (open standard)" = MCOS (open protocol + replaceable model + cross-vendor plugins). Rewrote "What is still missing" from "a Mobile Command Bus" to "an *open, model-agnostic* Mobile Command Bus standard," and added an honest acknowledgment that MCOS is a sandboxed third-party app — the bet is "open standard coexists with OS vendors" (like the open web vs native APIs), not "out-build the OS." (V2) Aligned the over-promising taglines with the real addressable surface: [00 §1](./docs/en/00-vision.md) "every capability on a phone" → "the capabilities a phone exposes through cooperative APIs"; [00 §12](./docs/en/00-vision.md) "Make every app on the phone a command" → "Make every cooperative capability…"; `README.md` + `README.zh-CN.md` taglines updated likewise; `README.zh-CN.md` competitive table updated to the two-row (OS-integrated vs open-standard) form. This aligns with the existing [00 §6 Non-Goal #3](./docs/en/00-vision.md) ("not a guarantee that every third-party app can be driven without cooperation") which the taglines previously contradicted. **M-class (MCP adapter P3→P2-spike/P3-production split):** the MCP adapter was uniformly labeled P3 across all docs, but [10 §11.2](./docs/en/10-roadmap.md) already listed "MCP adapter | Usable" as a V1/P2 metric — a pre-existing contradiction the docs papered over. Per the user's strategic decision (move the ecosystem-adoption thesis test earlier, but as a controlled spike — not wholesale P3→P2), split MCP into: (a) **P2 bridge spike** — validates [02 §12.4](./docs/en/02-command-protocol.md) schema conversion + the adoption thesis using user-configured trusted servers only; (b) **P3 production adapter** — robust connection management, per-server secrets UI, egress enforcement on remote servers (all depend on P2 process isolation). Implemented by: (M1) adding a `P2 -.->|bridge spike| MCP_SPIKE` dotted edge to the [§2.2 phase dependency graph](./docs/en/10-roadmap.md) + a `p2-14` Gantt task in §5.0 + decomposing the S7+ vertical-slice row into S7 (Workflow+Memory) / S8 (MCP spike); (M2) adding [§5.7 "Explicit Non-Goals for V1"](./docs/en/10-roadmap.md) (mirroring the §4.5 MVP non-goals guardrail) with explicit spike scope limitations + a §6.2 dependency note explaining why the production adapter stays P3 (process isolation dependency); (M3) updating all 6 MCP phase-label locations to "P2 spike / P3 production": [04 §10](./docs/en/04-plugin-sdk.md) + [§17 table](./docs/en/04-plugin-sdk.md), [11 §2 + §5](./docs/en/11-implementation-status.md), [REPOSITORIES §3](./docs/en/REPOSITORIES.md), [00 §5 module tree](./docs/en/00-vision.md). Also added a "Platform competition (Google/Apple)" row to the [10 §10.0/§10.1 risk register](./docs/en/10-roadmap.md) (rated 🔴 Critical — high severity, high probability — with mitigation via open-standard differentiation + early MCP spike + dogfood). All changes mirrored to ZH. Rationale: the production adapter cannot safely ship before process isolation (P3) because remote MCP servers are network tools — shipping them under MVP/P2's in-process "security theater" model would expand the documented MVP limitation to network-reaching tools, which is strictly worse.

### Docs — Cross-document consistency pass round 5 (Q1, 1 issue fixed across EN + ZH)
- Fixed **1 code-quality defect** found during a final full-doc-set structural scan (00–11 complete). **Q1 (curly/smart quotes inside code blocks):** [08-security.md](./docs/en/08-security.md) (EN + ZH mirror) contained Unicode curly quotes `“ ”` (`U+201C`/`U+201D`) inside fenced code blocks — 26 lines each side (52 total). The affected code spans: §1.2 mermaid flowchart node labels (`U[“User…”]` etc.), §4.0/§6.0 Kotlin comments, §10.1 `Permission` enum + `decideConfirmation` params, and §12.0/§12.1 `decideEgress()` / `globMatch()` pseudocode where `DENY(“kill_switch_active”)`, `startsWith(“https://”)`, and `pattern == “*”` used curly quotes as Kotlin/Python string delimiters — these would not compile, violating [10 §12.1](./docs/en/10-roadmap.md) "code blocks must be byte-identical / copy-paste-compilable." Root cause: editor auto-correction (smart quotes) was active when the code blocks were authored. Notably, EN and ZH were *both* affected identically, so the [10 §12.1](./docs/en/10-roadmap.md) EN↔ZH structural-parity check could not detect this ("consistently wrong on both sides") — confirming the need for a code-block-content linter beyond parity counting. Fixed by replacing all `“`/`”` with ASCII `"` inside code fences only (prose curly quotes outside code blocks are correct typography and were left untouched). Verification: full re-scan of `docs/en/` + `docs/zh/` confirms **zero** curly quotes remain in any code block across all 24 markdown files.

### Docs — Cross-document consistency pass round 4 (R1–R5, 5 issues fixed across EN + ZH)
- Fixed **5 cross-document drift issues** found during a full-repo read (00–11 + fixtures + root files), across all affected English and Chinese mirror files. **R1 (hard error — fixture counts written backwards):** [10-roadmap.md](./docs/en/10-roadmap.md) §3.2 said "3 positive + 5 negative" and §16.0 listed Positive=`01`–`03`/Count 3, Negative=`04`–`08`/Count 5 — both exactly reversed. The actual fixtures directory has **5 positive (01–05) + 3 negative (06–08)**, confirmed by `docs/fixtures/README.md` and [02 §16.1](./docs/en/02-command-protocol.md). Fixed both locations to 5 positive (01–05) / 3 negative (06–08). **R2 (scope overreach):** [10 §4.2](./docs/en/10-roadmap.md) Parser P1 scope listed "pipelines" alongside "commands, multi-statement", but [02 §17](./docs/en/02-command-protocol.md) explicitly classifies `a() | b()` pipelines as a Future Extension (non-normative) and [02 §18](./docs/en/02-command-protocol.md) EBNF grammar has no pipe operator — P1 must not implement features the protocol defers. Removed "pipelines" from P1 scope, moved it to the "Explicitly NOT in P1" column with cross-ref. **R3 (command surface lacks single source of truth):** the `files` plugin command set was inconsistent across 3 locations — [04 §17](./docs/en/04-plugin-sdk.md) had `file.list, file.search, photo.search, photo.compress`; [10 §4.3](./docs/en/10-roadmap.md) dropped `file.search`; [10 §4.3.1](./docs/en/10-roadmap.md) used fictional `file.read`/`file.write` instead. Additionally §4.3.1 introduced `device.*` commands (battery/wifi/screen/volume/location/brightness) and `sys.clipboard`/`sys.openUrl`/`sys.vibrate` that had no descriptor, no plugin entry, and appeared nowhere else — and `device` is not a reserved namespace root ([02 §4.3](./docs/en/02-command-protocol.md) reserves only `sys`/`mcp`/`mcos`/`std`). Fixed by: (a) designating [04 §17](./docs/en/04-plugin-sdk.md) as the **single source of truth** for the command surface (added explicit note); (b) expanding 04 §17 `mcos.plugin.system` to include `sys.clipboard`, `sys.openUrl`, `sys.vibrate`, and all 6 device queries renamed to `sys.device.*` (kept under the reserved `sys` root, owned by the system plugin); (c) aligning roadmap §4.3/§4.3.1 to match 04 §17 and cross-reference it; (d) adjusting the MVP metric from "≥20" to "≥15 documented built-in commands" (the reconciled list is 19 = 18 core + 1 optional, comfortably meeting ≥15). **R4 (dependency graph missing files):** [REPOSITORIES.md](./docs/en/REPOSITORIES.md) §1 mermaid graph and §2 module reference both omitted `mcos-plugin-files`, even though it is a P1 plugin ([Vision §5](./docs/en/00-vision.md), [04 §17](./docs/en/04-plugin-sdk.md), and REPOSITORIES §3 all mark it P1). Added `files` node + edges to the graph, added a §2 module reference entry, and moved `mcos-plugin-files` from §3 "Planned Modules" to §2 (it is part of the P1 reference set, not a later-phase plan). **R5 (stale Gantt dates):** [10 §4.0/§5.0](./docs/en/10-roadmap.md) Gantt charts used absolute dates (`2026-01-01`, `2026-04-01`) that predate the document's own last-updated date (2026-08-06), making them appear to be in the past. Added explicit notes that dates are illustrative relative week/month offsets (Week 1 / Month 1 = project kickoff), normalized the P2 anchor to `2026-01-01` (relative), and updated titles to clarify "(relative to kickoff)".

### Docs — Cross-document consistency pass round 3 (M1–M5 + MK1/MK2 + V1, 8 issues fixed across EN + ZH)
- Fixed **8 cross-document architecture and correctness issues** identified in a third full doc-set review, across all affected English and Chinese mirror files. **M-class (architecture-level — two independent designs were each internally consistent but contradicted each other when combined):** (M1+M2) **Tool catalog stability model contradiction between 06 and 07** — the central fix of this round. [06 §4.1](./docs/en/06-agent.md) described tool catalog retrieval as per-utterance (`embed(utterance)` → top-K=20, varies every turn), but [07 §3](./docs/en/07-memory.md) placed "commands block (active registry view subset) ← from 06 §4.1 retrieval" in Core Memory (pinned, cached prefix) and [07 §14.3](./docs/en/07-memory.md) placed "tool schemas" in the cached prompt prefix annotated "changes rarely." If tools are per-utterance retrieved, they are not stable → cannot be in the cached prefix → the ~90% cache discount narrative collapses. Additionally [06 §9.0](./docs/en/06-agent.md) system prompt section order placed static §4 Safety / §5 Output *after* dynamic §3 Memory, which is anti-cache (prompt cache requires all static content to form a continuous prefix). Fixed by: (a) splitting the tool catalog into a **stable core set** (builtin `sys.*`/`mcp.*`/`mcos.*`/`std.*` + recently-active + pinned commands → cached prefix §2a, changes only on plugin load/unload) and a **retrieval supplement** (`embed(utterance)` top-K minus core set → uncached suffix §2b, per-utterance); (b) rewriting 06 §9.0 section order to "static-first" (§1 Role → §4 Safety → §5 Output → §2a Tool[stable] → 【cache boundary】 → §2b Tool[supplement] → §3 Memory → history → message); (c) aligning 07 §3 commands block + §14.3 cache-prefix layout + §15.0/§15.3 to match 06 §9.0 word-for-word; (d) correcting 07 §15.0 combined-effect narrative (base window now in uncached suffix, not cache-discounted prefix). (M3) [07 §5.2](./docs/en/07-memory.md) Step 2 conflict detection used embedding similarity for same-path writes (e.g. `people.tom.phone` "+86-old" vs "+86-new" sim=0.92 → CONFLICT) — a modeling error: same-path writes are updates (Step 1 soft-delete handles them), and phone numbers have no meaningful embedding ("similar 0.92" is noise). Fixed by splitting Step 2 into: same-path write → UPDATED (no similarity check); cross-path semantic dedup within same `MemoryCategory` → similarity check (threshold > 0.85 → CONFLICT with `reason: "semantic_duplicate"`), with example changed to a genuine cross-path case (`places.公司地址` vs `places.office`). (M4) [07 §10.1](./docs/en/07-memory.md) key derivation used PBKDF2-HMAC-SHA256 (100K iterations) on the device Keystore master key — a KDF misuse: PBKDF2 is for low-entropy password stretching; the Keystore master key is already high-entropy hardware-random. Changed to HKDF-SHA256 (derives purpose-specific subkeys without stretching overhead). (M5) [02 §5.4](./docs/en/02-command-protocol.md) `resolveRef` contract was stale — still described the old nullable-String return ("returns a concrete id; if returns null → SCHEMA_VIOLATION") with no `Ambiguous` path, contradicting the `ResolveResult` sealed class (Resolved/Ambiguous/NotFound) upgraded in 07 §5.0/§6.0. Updated to reference the three-state mapping (Resolved → continue; Ambiguous → Clarify; NotFound → SCHEMA_VIOLATION). **MK-class (marketplace doc hygiene):** (MK1) [09 §5.1/§5.2](./docs/en/09-marketplace.md) had duplicate section numbers — the full "Automated CI Gates" (§5.1) and "Human Review Triggers" (§5.2) sections were each followed by a condensed "(Summary)" subsection with the same number, breaking TOC/anchor uniqueness; deleted both summary subsections, replaced with a single cross-reference blockquote. (MK2) [09 §11.4](./docs/en/09-marketplace.md) REST API error table mixed shared codes (`SCHEMA_VIOLATION`/`PERMISSION_DENIED`/`RATE_LIMITED`/`INTERNAL`) with HTTP-only codes (`UNAUTHENTICATED`/`NOT_FOUND`/`ALREADY_EXISTS`) without clarifying which are shared by coincidence vs. HTTP-only; added explicit note that the three HTTP-only codes never appear in the runtime DSL `McosErrorCode` enum. **V-class (terminology):** (V1) [00 §4](./docs/en/00-vision.md) used `P1`–`P8` for design principles ("P1 — Protocol First"), colliding with `P1`/`P2`/`P3` = implementation phases (P1=MVP/P2=V1/P3=V2) used everywhere else in the repo — readers could mistake "P1 — Protocol First" for "P1 (MVP) only does protocol." Renamed all 8 to **Principle 1–8**, added a terminology note in 00 §4, and updated [10 §2.1](./docs/en/10-roadmap.md) collision warning from one-directional to bidirectional (noting the rename).

### Docs — Workflow Engine expansion (merged into 05-workflow.md) + error code sync
- Expanded [docs/en/05-workflow.md](./docs/en/05-workflow.md) (+ Chinese mirror) from a ~491-line sketch (zero Kotlin types, most sections bullet/JSON-example only) into a ~1295-line **implementable engine design** by inserting 16 new subsections in place and resolving **11 gaps** discovered during cross-doc exploration. Strategy: **cross-reference rather than duplicate** — types already normative in [01](./docs/en/01-architecture.md) (`RuntimeEvent` 11 variants with step-level events, `McosErrorCode` enum as single source of truth, `ExecuteRequest.Payload.WorkflowRef`, `RunId`/`StepId`, 10-stage pipeline, `workflow` Scheduler queue, structured-concurrency rules), [02](./docs/en/02-command-protocol.md) (workflow IR wrapper §7.3, canonicalizer explicitly skips workflow body §7.5, `onFailure` §9.6), [03](./docs/en/03-runtime.md) (`WorkflowRef.body` opaque `JsonObject` §5.1, "re-enters Permission Kernel + Executor" §10, `requiresDevices` per-step IoT mutex §8.5), [06](./docs/en/06-agent.md) (Planner emits workflow IR), [07](./docs/en/07-memory.md) (`$memory` in event filters §13), and [09](./docs/en/09-marketplace.md) (recipe artifact §8) are compressed to pointers; the new material is the engine-internal detail that was missing or only sketched. Key additions: §1.1 engine positioning (sub-component above the pipeline, not a pipeline stage) + §1.2 **compile-time/run-time separation architecture** (the central design decision — compile pass produces frozen `CompiledWorkflow` at load time; execute pass re-enters the 10-stage pipeline at Stage 6 Authorize per step, skipping redundant Parse/Canonicalize/Resolve while preserving per-step AuthStamp + Audit), §3.1 Run lifecycle state machine (Created→Running↔Paused→Succeeded/Failed/Cancelled with mermaid `stateDiagram-v2` + per-state event mapping to 01 §11.5), §4.0 **normative Kotlin types for the first time** (`CompiledWorkflow`, sealed `Step` with 9 variants, `Edge`, `JoinPolicy`, `RetryPolicy`, `WorkflowAction`, `ConfirmationPolicy`, sealed `Trigger` — previously the doc had zero Kotlin types), §4.3 parallel form canonicalization algorithm (compact→explicit normalization, normative pseudocode, resolving the two undocumented forms), §5.0 `invoke` field reference table + §5.4–5.8 (`switch`/`wait_delay`/`noop`/`confirm`/`compensate` — three of these previously had no examples at all) + §5.9 **predicate language specification** (9-operator table with typing rules + security constraints — previously "JSONLogic-like subset" with zero enumerated operators), §6.0 `$ref` path grammar (normative BNF + source table + missing-path behavior + resolution algorithm) + §6.4 **three-ref-token disambiguation** (`$ref` vs `$memory` vs `x-mcos-ref` — previously undefined relationship), §7.0 **error handling decision tree** (normative fixed-priority ordering of retry→on-error→join→compensation, resolving the previously-unspecified interaction between these four mechanisms) + §7.1–7.4 (retry `retryOn` code alignment with 01 §15.1, on-error edge `onError` semantics, step-level `compensate` vs workflow-level `onFailure` **semantic scope distinction** — `compensate` = per-step reverse-order rollback, `onFailure` = single workflow-level fallback, resolving the naming conflict between 05 §7.3 `compensate` and 02 §9.6 `onFailure` by scoping them differently) + §7.5 workflow error code details schemas, §8 join policy expansion (4-strategy sibling-cancellation semantics table + concurrency admission rules + `quorum:N` validation), §9 trigger expansion (manual JSON + event `$memory` arming/fire-time resolution + schedule `misfirePolicy` 3-variant table + §9.4 **voice trigger** as a `source` variant of manual, not a separate type — resolving the gap where `Source.VOICE` existed in 01 §11.6 but no voice trigger was defined), §10.1 **concurrency model & cancellation propagation** (run-scope `Job` hierarchy diagram + 3 cancellation modes + engine lifecycle — stateless, absent from 03 §3.1 boot order intentionally), §11.1–11.2 **normative compile + execute algorithms** (replacing the 13-line "Informative" sketch with full pseudocode; compile: parse→validate→canonicalize→resolve→cycle-detect→freeze; execute: frontier-based dispatch with per-step pipeline re-entry at Stage 6, compensation stack, terminal-state handling), §13 Planner emission rules (compile-checkability + `debug.allowPartial`), §14.1 **recipe envelope schema** (full field table + placeholder binding + security constraints, expanding 09 §8's 5-line sketch), §15 MVP vs V1 table (expanded from 9 to 19 rows, aligned with 03 §21), §17 testing matrix (12-category table replacing 4 bullets). **5 new workflow error codes added to 01 §15.1** (single source of truth, EN+ZH synced): `WORKFLOW_INVALID`, `MAX_ITERATIONS_EXCEEDED`, `COMPENSATION_FAILED`, `JOIN_FAILED`, `TRIGGER_MISFIRE` — bringing the enum from 12 to 17 codes. **11 gaps resolved:** compensation naming conflict (`compensate` vs `onFailure` → scoped distinctly), `requiresDevices` absent from 05 (now cross-referenced from 03 §8.5), no workflow error codes (5 added), pipeline re-entry point unspecified (Stage 6 Authorize, justified), `WorkflowRef.body` opaque parsing location (compile pass), three ref-token overlap (disambiguated), no voice trigger (added as manual `source` variant), no workflow-lifecycle `RuntimeEvent` variants (Run-level events reused, documented), engine absent from boot order (intentionally stateless, documented), parallel form canonicalization (algorithm specified), open IR encoding question (01 §19 #2 — JSON confirmed by this design). No content in 02/03/06/07/08/09 was modified; 01 §15.1/§15.2 received only the 5 new error code additions.

---

## [0.1.0] - 2026-08-06

*"Phase 0 — design docs + compilable multi-module skeleton."*

### Added — Design RFCs (`docs/`)
- **00-vision.md** — Mobile Command Bus positioning, principles, non-goals.
- **01-architecture.md** — Layered system, request lifecycle, process model.
- **02-command-protocol.md** *(MCOS-RFC-0001)* — Normative DSL / IR / type system / error codes.
- **03-runtime.md** — Parser, Registry, Executor, Permission Kernel, Scheduler, Audit (target design).
- **04-plugin-sdk.md** — Manifest, handler contracts, host services.
- **05-workflow.md** — Graph IR: sequence, parallel, retry, compensation, triggers.
- **06-agent.md** — AIProvider, Command Compiler, repair loop.
- **07-memory.md** — Profile layers, ref resolution, privacy.
- **08-security.md** — Threat model, defense in depth, confirmation UX.
- **09-marketplace.md** — Signing, install/update, recipe store.
- **10-roadmap.md** — P0 → P4 phased plan.
- **Golden fixtures** `docs/fixtures/01`–`05` — DSL ↔ IR round-trip cases.

### Added — Code skeleton
- **`mcos-sdk`** — `McosPlugin`, `CommandHandler`, `CommandDescriptor`, `SideEffectClass`, `CommandResult`, `ExecutionContext` (reduced contract).
- **`mcos-runtime`** — IR types (`IrNode`/`IrInvoke`/`IrSequence`) + `DslParser` stub + fixture-presence test.
- **`mcos-android`** — Jetpack Compose `MainActivity` shell.
- **`mcos-plugin-hello`** — Reference plugin with a genuinely working `hello.world` handler + `plugin.json` manifest.
- **`mcos-plugin-system`** — `sys.notify` / `sys.share` descriptors (stub handlers).
- **`mcos-plugin-camera`** — `camera.capture` / `camera.scan` descriptors (stub handlers).
- Gradle multi-module build (`settings.gradle.kts`, version catalog `gradle/libs.versions.toml`, Kotlin 2.0.21, AGP 8.7.3, JDK 17).

### Added — Project
- [LICENSE](./LICENSE) — Apache License 2.0.
- [CONTRIBUTING.md](./CONTRIBUTING.md) — contribution principles and protocol-change workflow.
- [README.md](./README.md) — project overview and doc index.

### Known gaps (tracked in [docs/en/11-implementation-status.md](./docs/en/11-implementation-status.md))
- `DslParser` is a stub returning `not_implemented` — the P1 entry point.
- Registry / Executor / Permission Kernel / Audit / Workflow / Event Bus / Memory are spec-only.
- `mcos-plugin-system` and `mcos-plugin-camera` handlers return `status: stub`.

---

### Versioning note

The project version (`0.1.0`), the document/RFC version (`0.1.0`), and the DSL language version (`"0.1"`, a `MAJOR.MINOR` shorthand — see [02-command-protocol.md](./docs/en/02-command-protocol.md) §14.2) are three distinct versioning axes. First-party plugin manifests may independently declare command SemVer (e.g. `hello.world` at `1.0.0`) since command contracts can stabilize before the runtime reaches 1.0.
