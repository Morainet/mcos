<?xml version="1.0" encoding="utf-8"?>
<!-- Demo shell manifest — deliberately minimal. All host-runtime manifest
     surface (uses-permissions, ScheduleAlarmReceiver, BootReceiver, the
     FileProvider) is declared by the mcos-android-sdk library manifest and
     merged in at build time, so an integrating app gets it for free and this
     file only describes the demo UI itself. -->
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:name=".McosApplication"
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="MCOS Shell"
        android:supportsRtl="true"
        android:theme="@android:style/Theme.Material.Light.NoActionBar">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>
